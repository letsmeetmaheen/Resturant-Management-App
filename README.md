
  # SmartDine App UI Design

  This is a code bundle for SmartDine App UI Design. The original project is available at https://www.figma.com/design/1qe7dOj9sCOWr0EVIzaMXq/SmartDine-App-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  